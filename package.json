{
	"private": true,
	"scripts": {
		"build": "rimraf dist && tsc",
		"start": "node dist/main",
		"dev:build": "tsc -w",
		"dev:watch": "ts-node-dev --respawn --rs src/main.ts",
		"test": "jest"
	},
	"devDependencies": {
		"@types/jest": "^27.0.1",
		"@types/node": "^14",
		"jest": "^27.1",
		"rimraf": "^3.0.2",
		"ts-jest": "^27.0",
		"ts-node-dev": "^1.1.8",
		"typescript": "^4.4"
	},
	"jest": {
		"preset": "ts-jest",
		"testEnvironment": "node",
		"globals": {
			"ts-jest": {
				"tsconfig": "tests/tsconfig.json"
			}
		}
	}
}
