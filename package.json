{
	"name": "@username/package-name",
	"version": "1.0.0",
	"description": "Describe your package here.",
	"author": "Your Name <email@example.com> (https://website.org)",
	"repository": "github:username/repository",
	"bugs": "https://github.com/username/repository/issues",
	"license": "MIT",
	"main": "dist/main.js",
	"types": "dist/main.d.ts",
	"files": [
		"dist/**/*"
	],
	"keywords": [],
	"scripts": {
		"build": "rimraf dist && tsc",
		"start": "node dist/main",
		"dev:build": "tsc -w",
		"dev:watch": "ts-node-dev --respawn --rs src/main.ts",
		"test": "jest"
	},
	"devDependencies": {
		"@types/jest": "^27.0.1",
		"@types/node": "^14",
		"jest": "^27.1",
		"rimraf": "^3.0.2",
		"ts-jest": "^27.0",
		"ts-node-dev": "^1.1.8",
		"typescript": "^4.4"
	},
	"jest": {
		"preset": "ts-jest",
		"testEnvironment": "node",
		"globals": {
			"ts-jest": {
				"tsconfig": "tests/tsconfig.json"
			}
		}
	}
}
